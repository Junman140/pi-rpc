PI_RPC_URL=http://localhost:8000
NETWORK_PASSPHRASE=Pi Testnet

# Faucet account used to fund new test accounts (backend only)
FAUCET_PUBLIC=GCAO7I2ZBIEFXNYD3KZFA3UXLMDVCYUG6CTCYG56746LZB5MIUFIIBJ5
FAUCET_SECRET=SCB2NN44YEITKM2TEXCTPP3LB33DXFI3M7PKCVJU24UFELY6TTFGOD44

# Optional: protect /admin/* endpoints (deploy/invoke). Recommended if running on shared networks.
ADMIN_TOKEN=change_me_please

# Admin-only endpoints protection (required to deploy/invoke contracts via backend).
# Choose any long random string. The frontend never needs this unless you want UI buttons to call admin endpoints.
ADMIN_TOKEN=change-me-to-a-long-random-string

# Optional classic network parameters (leave default unless needed)
HORIZON_URL=

